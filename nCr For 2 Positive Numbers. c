#include <stdio.h>
int ncr(int n, int r)
{
    if(r==0 || r==n)
        return 1;
    else
        return ncr(n-1, r-1) + ncr(n-1, r);
}
int main()
{
    int n, r;
    printf("Enter n and r values: ");
    scanf("%d %d", &n, &r);
    printf("nCr = %d", ncr(n,r));
    return 0;
}
